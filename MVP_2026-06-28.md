# PaperFlow MVP — 2026-06-28

## Demo promise

사용자가 다음을 제공한다.

1. journal/user information
2. core message
3. data/result files + file descriptions
4. research plan/related documents/references
5. optional outline

PaperFlow는 관련 문헌을 조사하고, 연구 상태와 잠정 logic graph를 만든 뒤, graph를 완성하기 위한 질문을 한다. 답변 후 final logic graph를 확정하고 전체 논문 초안을 생성하여 DOCX와 read-only preview로 제공한다.

## P0 — 반드시 완료

> 상태 표기: [x] 완료 · [~] 부분(기존 엔진 재사용 또는 LLM 키 필요) · [ ] 미완료.
> (2026-06-26 갱신 — feature/research-reconstruction-mvp)

### Input

- [x] 새 프로젝트 생성 (`/api/projects/create`)
- [x] journal/user info 입력
- [x] core message 입력
- [x] research documents/reference 업로드 (`/api/projects/upload`)
- [x] data/result files 업로드
- [x] 각 data file의 설명 저장 (`data_notes.json`)
- [x] outline optional 처리 (ingest가 outline 부재를 허용)

### Research reconstruction

- [x] `ResearchState` schema
- [x] `EvidenceInventory` schema
- [~] uploaded documents text extraction (md/txt/dat/csv는 profiling; PDF 본문 추출은 향후)
- [x] CSV/XLSX basic profiling (`reconstruct/profile_data.py`)
- [x] file description + inferred role 결합
- [x] `research_state.json` 생성
- [x] `evidence_inventory.json` 생성

### Literature

- [~] field reconstruction search (기존 OpenAlex litsearch 재사용)
- [~] representative paper list (litsearch 결과)
- [~] claim별 literature query 생성 (기존 litsearch/run.py, LLM 키 필요)
- [~] DOI/OpenAlex identifier 저장 (FoundRef)

### Question loop

- [~] preliminary logic graph 생성 (claim_graph.build — LLM 키 필요; 없으면 skip)
- [x] graph gap 탐지 (`question/gaps.py`)
- [x] question value 계산 (`question/rank.py`)
- [x] 질문 batch 표시 (`/api/projects/questions`, 가변 개수·작은 batch)
- [x] 답변 저장 (answer.json MERGE)
- [x] 답변 반영 후 graph 재생성 (plan()이 답변 포함해 재빌드)
- [x] 모든 질문 미응답이어도 generation 가능 (hard gate 제거)
- [x] load-bearing gap은 warning으로 유지

### Manuscript

- [x] final logic graph 저장 (`claim_graph.json`)
- [~] section/paragraph contracts 생성 (기존 contracts.py, LLM 키 필요)
- [~] Introduction / Method / Result / Discussion / Conclusion / Abstract
      (기존 writer 파이프라인; research_state+evidence를 입력으로 소비, LLM 키 필요)
- [x] Figure/Table placeholders (`figures/plan.py`)
- [x] Figure/Table generation prompts (caption_draft + generation_prompt)

### Output

- [x] canonical `manuscript.json` (`output/manuscript_state.py`)
- [x] DOCX export (`output/write_docx.py`)
- [x] generated file list (`/api/projects/output/list`)
- [x] file click → right-side read-only preview (`/api/projects/output/preview` + UI pane)
- [x] grounding report (`grounding_report.json` + manuscript.json.grounding)

## P1 — 시간이 남으면

- [ ] citation existence checker UI badge
- [ ] numerical grounding badge
- [ ] section별 regeneration
- [ ] question fatigue analytics
- [ ] token/cost report

## 이번 MVP에서 하지 않음

- 본문 직접 편집
- 실제 figure 이미지 생성
- 실시간 공동편집
- 완전한 citation entailment
- 모든 연구 분야 범용화
- reviewer multi-agent

## 추천 구현 순서

### 1. Backend state

```text
schemas/research_state.py
schemas/evidence_inventory.py
reconstruct/profile_data.py
reconstruct/build_state.py
```

### 2. Question flow

```text
question/rank.py
question/loop.py
server/app.py
```

### 3. Graph and writing

```text
compile/claim_graph.py
compile/contracts.py
compile/writer.py
```

### 4. Output

```text
output/manuscript_state.py
output/write_docx.py
server/static/*
```

## Demo acceptance test

한 개의 실제 연구 프로젝트에서:

- outline 파일을 제거한다.
- research plan과 data만 업로드한다.
- PaperFlow가 research state를 생성한다.
- 최소 한 차례 질문/답변을 수행한다.
- final logic graph를 만든다.
- 전체 초안을 생성한다.
- DOCX가 다운로드된다.
- 같은 내용이 오른쪽 preview에 표시된다.

이 흐름이 한 번 끊김 없이 동작하면 MVP 성공으로 본다.

## 구현 노트 (2026-06-26)

- **Deterministic(키 없이 동작·테스트됨):** reconstruction(프로파일링·skeleton), graph-gap
  질문 loop, manuscript.json/HTML preview/DOCX/grounding report, 모든 서버 입출력 엔드포인트.
- **LLM 키 필요(provider key 있을 때):** research_state 보강, preliminary/final claim graph,
  litsearch, section contracts·writer·validator. 키가 없으면 해당 단계는 graceful skip(예:
  prelim graph 없이 reconstruction gap만으로 질문)되며 파이프라인은 끊기지 않는다.
- **실행:** `pip install -e engine` 후 `paperflow reconstruct <project>` (오프라인),
  `paperflow serve` (웹 UI), `paperflow run <project>` (전체, 키 필요).
- **테스트:** `cd engine && pytest` — schema/profiling/reconstruction/question/manuscript/
  docx/preview/server 36 케이스, 전부 오프라인.
- **남은 P0:** 위 LLM-gated 단계는 provider key가 있는 환경에서의 end-to-end 검증이 필요
  (이 환경에는 키가 없어 오프라인 부분만 실증). PDF 외 PDF 문서 본문 텍스트 추출은 향후.

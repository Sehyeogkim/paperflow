# paperflow 데모 사용법 🧪

> **paperflow** = 연구 자료를 업로드하고 핵심을 채팅으로 설명하면, 근거가 추적되는 **논문 초안(.docx)**을 만들어주는 도구. ChatGPT처럼 채팅하면 됩니다.

이 문서대로만 따라 하면 친구 컴퓨터에서 데모를 돌릴 수 있어요. 10분이면 충분합니다.

---

## 0. 준비물

- **git** (설치: https://git-scm.com)
- **Python 3.10 이상** (확인: 터미널에서 `python3 --version`)
- **OpenAI API 키** 1개 — https://platform.openai.com/api-keys 에서 발급
  - 데모 한 바퀴(논문 초안 1개)에 대략 **$0.2~0.3** 정도 듭니다. 결제수단 등록 필요.

> 윈도우면 PowerShell 대신 **Git Bash** 또는 WSL 권장. 아래 명령은 macOS/Linux 기준이에요.

---

## 1. 코드 받기 (git clone)

```bash
git clone https://github.com/Sehyeogkim/paperflow.git
cd paperflow
git checkout feature/research-reconstruction-mvp   # ← MVP가 들어있는 브랜치
```

---

## 2. 설치

```bash
cd engine

# (권장) 가상환경 만들기
python3 -m venv .venv
source .venv/bin/activate          # 윈도우(Git Bash): source .venv/Scripts/activate

# 엔진 + 웹서버 설치 (fastapi·uvicorn 포함)
pip install -e .
```

> 설치에 1~2분 걸립니다. `paperflow` 명령이 생깁니다.

---

## 3. API 키 넣기 (.env)

```bash
# engine/ 폴더 안에서
cp .env.example .env
```

그다음 `engine/.env` 파일을 열어 **본인 OpenAI 키**만 넣으면 됩니다:

```
OPENAI_API_KEY=sk-....(본인 키)....
```

나머지는 기본값(전부 `gpt-4o-mini`)으로 두면 잘 돌아갑니다.
`.env`는 git에 안 올라가니(=gitignore) 키가 새어나갈 걱정 없어요.

> gpt-5-mini 접근 권한이 있으면 `.env`의 FAST/WRITER/REASONING 3줄을 `openai:gpt-5-mini`로 바꾸면 품질이 더 좋아져요. (없어도 데모는 gpt-4o-mini로 잘 됩니다.)

---

## 4. 실행

```bash
# engine/ 폴더, 가상환경 켠 상태에서
paperflow serve
```

이렇게 뜨면 성공:

```
Uvicorn running on http://127.0.0.1:8765
```

브라우저로 **http://127.0.0.1:8765** 열기.
키가 잘못됐으면 채팅을 보낼 때 오류 메시지가 떠요 — 그러면 `.env`의 `OPENAI_API_KEY`를 확인하고 서버를 재시작하세요.

---

## 5. 데모 따라 하기 🎬

1. **가운데 입력창에 연구를 한국어/영어로 자유롭게 설명**하고 보냅니다. 예:
   > "관상동맥 플라크 파열 위험을 1,000개 샘플 FSI로 분석했고, ΔPSS/strength 기반 파열위험지표를 정의하고 재료물성이 지배적임을 보이고 싶어. Computers in Biology and Medicine 저널 타깃."

2. **AI(orchestrator)가 자동으로**
   - 새 프로젝트 + 세션을 만들고 (이름도 알아서 지어줌)
   - 설명에서 **저널정보·핵심메시지·아웃라인을 추출**해 카드로 정리하고
   - 부족한 부분을 **질문**하고, **"데이터 더 있나요?"**도 물어봅니다.

3. 부족한 건 카드의 **Edit / Attach** 버튼으로 채우거나, 아래 입력창에 추가로 답하면 AI가 반영해요.
   (데이터 파일은 우측 패널 **Data** 탭에서 CSV 등을 올리면 됩니다 — 없어도 데모는 진행돼요.)

4. **"Looks right — research prior work →"** 누르면
   - OpenAlex로 선행연구를 찾고(약 1~2분)
   - 심사자 관점의 **추가 질문**들이 뜹니다. 답하거나 **"Let me decide"**로 넘기세요.

5. **"Generate the paper →"** 누르면
   - 논리그래프 구성 → 섹션 작성 → 인용 채우기 → 출력까지 **실시간 로그**로 진행(약 2~3분).
   - 끝나면 **결과 카드**에서 **DOCX / PDF / .tex** 다운로드, **"Open full draft"**로 우측에 미리보기.

> 전체 한 바퀴: 입력 제외 약 **5분**, 비용 약 **$0.2~0.3**.

---

## 6. 알아두면 좋은 기능

- **프로젝트 = 논문 1편**, 그 안에 **세션(=trial) 여러 개**. 왼쪽 사이드바에서 프로젝트를 누르면 펼쳐집니다.
- **예전 세션 클릭 → 그날의 채팅 기록이 그대로 복원**됩니다 (질문·답변·생성로그·결과까지).
- **＋ new trial**: 같은 입력/데이터(source 공유)로 **다시 다른 버전**을 뽑을 수 있어요.
- **🗑 삭제**: 프로젝트·세션 위에 마우스 올리면 휴지통 아이콘. 지워도 `.trash`로 이동(복구 가능)이라 안전.
- 아래 입력창은 **gpt-4o-mini 일반 채팅**이라 아무거나 물어봐도 됩니다.

---

## 7. 문제 해결

| 증상 | 해결 |
|---|---|
| 채팅 보낼 때 키/인증 오류 | `engine/.env`의 `OPENAI_API_KEY` 확인 후 서버 재시작 |
| `paperflow: command not found` | 가상환경 켜기(`source .venv/bin/activate`) + `pip install -e ".[server]"` 다시 |
| 8765 포트 사용 중 | 기존 서버 종료: `lsof -ti:8765 \| xargs kill` |
| `model ... does not exist` 오류 | gpt-5-mini 접근 없음 → `.env`의 모델을 전부 `openai:gpt-4o-mini`로 |
| diagnose/generate가 너무 느림 | 정상(문헌검색·LLM 호출). 1~3분 기다리면 됩니다 |
| 화면이 안 바뀜 | 브라우저 강력 새로고침 `Cmd/Ctrl + Shift + R` |

---

## 8. 종료

터미널에서 `Ctrl + C` → 서버 종료. 가상환경 끄려면 `deactivate`.

문제 있으면 세혁이한테 연락 🙌
